# OpenMap Unifier - Core Dependencies
# Python 3.12 compatible

customtkinter>=5.2.2
Pillow>=10.0.0
requests>=2.32.0
shapely>=2.1.0
pyproj>=3.7.0
tqdm>=4.66.0
packaging>=23.0

# Optional: NTLM authentication for corporate proxies
# Uncomment if needed in your environment
# requests-ntlm>=1.2.0

# Optional: crop Sentinel-2 / ESA WorldCover to the drawn polygon (windowed COG
# reads). Without it those sources download the whole tile instead; the Bayern
# tile / WMS / CIR downloads do NOT need this.
# rasterio>=1.3.0
